# CLAUDE.md

## Git 규칙

- `git add -A` / `git add .` 사용 금지
- 커밋할 파일을 항상 하나씩 명시적으로 지정할 것
- 이 저장소는 **공개 저장소**이므로 사업자 서류·계약서·인증서 등이 실수로
  커밋되면 즉시 유출됨

`.gitignore`에 사업자 서류 패턴(`*사업자등록증*`, `*계약서*`, `*.pdf` 등)을
등록해 두었지만, 이는 2차 방어선이다. 새로운 형식의 민감 문서는 패턴에
걸리지 않을 수 있으므로 파일을 명시적으로 지정하는 습관이 1차 방어선이다.

커밋 전 `git diff --cached --name-only`로 스테이징된 파일을 눈으로 확인할 것.

## 배포

- 배포 타겟은 **Firebase App Hosting** (프로젝트 `sniff-by-hatch-app`,
  백엔드 `sniff-web-server`). Vercel·Netlify 아님.
- 프로덕션 환경변수는 `apphosting.yaml`에서 주입한다.
- `NEXT_PUBLIC_*` 변수는 **빌드 타임에 번들로 인라인**되므로, 값이 비어 있는
  상태로 커밋하면 그대로 롤아웃되어 프로덕션이 망가진 채 굳는다.
  키를 아직 발급받지 못했다면 빈 값으로 넣지 말고 주석으로 남길 것.
- 시크릿(웹훅 시크릿 등)은 `apphosting.yaml`에 평문으로 넣지 말고
  Cloud Secret Manager에 등록한 뒤 `secret:` 으로 참조할 것.

## 사업자정보

전자상거래법·PG 심사 요건상 상시 노출해야 하는 사업자정보는
`src/lib/business-info.ts` **한 곳에만** 정의하고,
`src/components/layout/BusinessInfo.tsx`를 통해 푸터·결제페이지가 공유한다.
새 화면에 사업자정보가 필요하면 값을 복사하지 말고 이 컴포넌트를 재사용할 것.

주소·대표자명 등은 **사업자등록증과 글자 단위로 일치**해야 한다
(불일치 시 PG 심사 반송).

## 다국어

- 문구 추가·수정 시 `messages/ko.json`과 `messages/en.json`을 **항상 함께**
  갱신할 것. 한쪽에만 키가 있으면 빌드 시 `MISSING_MESSAGE` 에러가 나고,
  화면에는 키 이름이 그대로 노출된다.
- 키 파리티 확인은 두 파일의 키 집합을 비교해서 양방향 차집합이 비어 있는지
  보면 된다.
